name: Angular Deploy gh-pages Actions
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: AhsanAyaz/angular-deploy-gh-pages-actions@v1.3.2
        with:
          github_access_token: ${{ secrets.GITHUB_TOKEN }}
          angular_project_dir: app/src 
